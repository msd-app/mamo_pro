<% if (errors.length > 0) { %>
  <div class="panel">
    <ul class="errors">
      <% errors.forEach(error => { %>
        <li><%= error %></li>
      <% }); %>
    </ul>
  </div>
<% } %>


<style>
.panel {
  border: 1px solid red;
  color: red;
  background-color: white;
  margin: 10px;
}
</style>